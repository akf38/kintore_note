<% visiter = notification.visiter %>
<% item = notification.tweet %>

<div class="user-view clearfix my-2">
  <%= link_to user_path(notification.visiter.id) do %>
    <%= attachment_image_tag visiter, :image, fallback: "no_image.jpg", size: "50x50", class: 'rounded-circle' %>
  <% end %>
  <%= notification_form(notification) %><span class="moderate-font"><%= "(#{time_ago_in_words(notification.created_at)}前)" %></span>
</div>
<hr>
